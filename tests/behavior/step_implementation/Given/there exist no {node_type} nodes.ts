import {Given} from "@badeball/cypress-cucumber-preprocessor"

Given('there exist no {string} nodes', (nodeType: string) => {
    // TODO currently it is assumed that no nodes exists -> needs to be evaluated -> when there are any nodes they need to be deleted
})
