import {Given} from "@badeball/cypress-cucumber-preprocessor"

Given('there exist {string} nodes', (nodeType: string) => {
    // TODO currently it is assumed that nodes exists -> needs to be evaluated -> when there are no nodes then we have to create at least one
})
