html
    link(rel='stylesheet', href='/stylesheets/brands.css')

    head
        title #{title}

    body
        h1.text-3xl.font-bold.underline= message

    ul.list-disc.list-inside
        each item in collection
            li #{item.name} (##{item.id})

    div.relative
    p(class="absolute bottom-0 right-0 p-4 border-2 border-purple-200 text-purple-600") Debug: #{collection.length} items loaded
