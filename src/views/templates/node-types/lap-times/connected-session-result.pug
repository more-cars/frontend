if sessionResult
    section(data-testid="session-result-section" class="mb-8")
        h2(class="text-2xl mb-2") Session Result
        p(class="mb-4") This lap time was achieved in the context of the following session result:

        ul
            - nodeItem = sessionResult
            - nodeType = 'session-result'
            - nodeUrlPath = 'session-results/' + nodeItem.id
            - thumbnail = relationships.session_result.thumbnails.get(nodeItem.id)
            - nodeProperties = relationships.session_result.node_properties
            - itemHeadline = nodeItem.driver_name
            include ../../shared/list-node-item
