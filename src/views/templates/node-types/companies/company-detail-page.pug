extends ../../node-types/detail-page

block sub_headline
    - company = node.data
    | #{company.founded}-#{company.defunct}

block relationships
    - company = node.data

    - brands = relationships.brands.items
    include connected-brands

    - images = relationships.images.items
    include ../../shared/connected-images
