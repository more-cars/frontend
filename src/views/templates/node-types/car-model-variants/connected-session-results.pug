if sessionResults.length > 0
    section(data-testid="session-result-section" class="mb-8")
        h2(class="text-2xl mb-2") Session Results
        p(class="mb-4") The car model variant #[b #{carModelVariant.name}] has the following #{sessionResults.length} session results:

        ul
            each nodeItem in sessionResults
                - nodeType = 'session-result'
                - nodeUrlPath = 'session-results/' + nodeItem.id
                - thumbnail = relationships.session_results.thumbnails.get(nodeItem.id)
                - nodeProperties = relationships.session_results.node_properties
                - itemHeadline = nodeItem.driver_name
                include ../../shared/list-node-item
