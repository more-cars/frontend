if carModels.length > 0
    section(aria-label="car models")
        h2(class="text-2xl mb-4 text-center") Car Models
            span  (#{carModels.length})

        ul(
            class="grid sm-grid-cols-1 lg:grid-cols-2 gap-4 m-auto"
            aria-label="List of all Car Models that are attached to the Brand"
            data-testid="car-model-list"
        )
            each item in carModels
                li(class="mb-4 bg-stone-100 rounded-md inset-shadow-sm" data-testid=`car-model-${item.id}`)
                    div(class="grid grid-flow-col grid-cols-4 gap-4")
                        include ../nodes/thumbnail
                        div(class="col-span-3")
                            a(class="inline-block font-bold text-xl mt-3 underline" href=`/car-models/${item.id}`)
                                span(aria-label="name") #{item.name}
                            if item.internal_code
                                span(aria-label="internal code")  (#{item.internal_code})
                        - primaryProperties = relationships.car_models.primary_properties
                        include ../nodes/primary-properties
