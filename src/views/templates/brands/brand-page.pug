extends ../../layouts/base/layout

block main
    h1(class="mb-2 text-3xl font-bold underline") #{brand.name}
    include fact-sheet

block footer
    p Footer area...

block debug
    div(class="absolute bottom-0 right-0 p-4 border-2 border-purple-200 text-purple-600")
        p Brand ##{brand.id} loaded
