section(data-testid="image-section" class="mb-8")
    h2(class="text-2xl mb-2") Image Gallery
    p(class="mb-4") There are #{images.length} images available for the #[b #{item.name}]:
    ul
        each item in images
            include ../images/image-list-item
