extends ../../layouts/base/layout

block main
    - raceTrack = node.data
    div(class="grid grid-cols-2 gap-8 items-start")
        div(data-testid="main-area-primary")
            h1(class=`text-left mb-1 text-3xl font-bold`) #{raceTrack.name}
            div(class="mb-4 text-sm") #{raceTrack.location}

            - item = raceTrack
            include ../nodes/fact-sheet

        div(data-testid="main-area-secondary")

block footer
    p Footer area...
